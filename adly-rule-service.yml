server:
  port: ${PORT:8500}

security:
  oauth2:
    resource:
      userInfoUri: http://localhost:9191/uaa/user

spring:
  data:
    cassandra:
      keyspace-name: adly
      port: 9042
      contact-points: localhost
  cloud:
    stream:
      bindings:
        input:
          destination: beacons
          group: beacons-group
          durableSubscription: true

