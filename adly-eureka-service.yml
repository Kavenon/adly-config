server:
  port: ${PORT:8761}

hystrix:
  command:
    default:
      execution:
        timeout:
          enabled: false

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
  instance:
    prefer-ip-address: true

