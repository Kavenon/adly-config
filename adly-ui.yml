server:
  port: 8080

spring:
  aop:
    proxy-target-class: true

zuul:
  ignored-services: '*'
  routes:
    adly-edge:
      path: /api/**
      serviceId: adly-edge
    adly-auth:
      path: /uaa/**
      stripPrefix: false
      serviceId: adly-auth

